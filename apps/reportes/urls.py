"""
URLs para reportes
"""
from django.urls import path

app_name = 'reportes'

urlpatterns = [
    # path('general/', views.ReporteGeneralView.as_view(), name='general'),
    # path('ocupacion/', views.OcupacionView.as_view(), name='ocupacion'),
    # path('servicios/', views.ServiciosMasSolicitadosView.as_view(), name='servicios'),
]
